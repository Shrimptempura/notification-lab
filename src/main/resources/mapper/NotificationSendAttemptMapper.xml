<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="portfolio.notification_lab.mapper.NotificationSendAttemptMapper">

    <insert id="insertSendAttempt"
            parameterType="portfolio.notification_lab.command.SendAttemptCommand">
        INSERT INTO notification_send_attempt (
            request_id,
            campaign_id,
            recipient_id,
            attempt_no,
            result_status,
            retryable,
            fail_reason
        ) VALUES (
            #{requestId},
            #{campaignId},
            #{recipientId},
            #{attemptNo},
            #{resultStatus},
            #{retryable},
            #{failReason}
        )
    </insert>


</mapper>